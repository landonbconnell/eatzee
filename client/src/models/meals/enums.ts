export enum Meals {
  breakfast,
  lunch,
  dinner,
}

export enum Variables {
  time,
  budget,
  food_mood,
  skill_level,
}

export enum Weekdays {
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
  Sunday,
}
